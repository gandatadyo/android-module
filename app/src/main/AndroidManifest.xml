<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aplikasi.androidmodule">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidModule"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.exam_reyclerviewloadmore.ExamRecyclerviewLoadMore"
            android:exported="false" />
        <activity
            android:name=".ui.exam_http.ExamRetrofit"
            android:exported="false" />
        <activity
            android:name=".ui.exam_interface.ExamInterface"
            android:exported="false" />
        <activity
            android:name=".ui.exam_http.ExamHttp"
            android:exported="true" />
        <activity
            android:name=".ui.exam_backgroundprocess.ExamBackgroundProcess"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>